# タスク完了時の確認事項
- Lua 設定の構文エラーがないか `nvim --headless -c "quit"` などで最低限確認する（必要に応じて Lazy の遅延読み込み後に該当プラグインを触ってエラーが出ないか手動確認）。
- 可能なら変更部分のキー操作/コマンドを手元で試す（例: 該当プラグインのパネルを開いて動作確認）。
- git 操作はユーザ指示があるまで実施しない。
- ファイル削除を行う場合は事前にユーザへ確認する。